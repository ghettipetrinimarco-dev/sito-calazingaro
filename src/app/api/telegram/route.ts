// Bot 1 — gestione menu via Telegram
// Logica completa da implementare (ref: sito-piccolo-hotel/app/api/telegram/route.ts)
// Flusso: webhook → Redis sessione → dialogo step → commit atomico Octokit → Vercel redeploy

import { NextResponse } from "next/server"

export async function POST() {
  // TODO: implementare
  return NextResponse.json({ ok: true })
}
